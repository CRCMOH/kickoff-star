import { useCareerStore } from '../store/careerStore'
import type { SundayLeagueClub } from '../types/youthWorld'
function meter(v:number){return Math.max(8,Math.min(100,Math.round(v*10)))}
export default function GrassrootsSelection({onChoose}:{onChoose:(club:SundayLeagueClub)=>void}){
 const world=useCareerStore(s=>s.youthWorld), clubs=world?.sundayClubs??[]
 return <div className="relative min-h-screen bg-ks-black px-5 py-8"><div className="relative z-10 max-w-md mx-auto"><div className="font-display tracking-widest text-[11px] text-ks-gold uppercase">choose your grassroots club</div><h1 className="font-display text-ks-ink text-2xl mt-2">Earn your place.</h1><p className="text-xs text-ks-muted mt-1 mb-5">You start in Division 3. Pick the club whose coaching, exposure and football level fit your journey, then complete its three-week trial.</p><div className="flex flex-col gap-3">{clubs.slice(0,6).map(club=><button key={club.id} onClick={()=>onChoose(club)} className="text-left rounded-2xl border border-ks-border bg-[#0f0f0d] px-5 py-4 hover:border-ks-gold"><div className="flex justify-between"><b className="font-display text-ks-ink">{club.name}</b><span className="text-[9px] uppercase text-ks-gold">Division 3</span></div><div className="grid grid-cols-3 gap-2 mt-3">{[['strength',club.strength],['coaching',club.coaching],['exposure',club.exposure]].map(([label,value])=><div key={String(label)}><div className="text-[8px] text-ks-muted uppercase">{label}</div><div className="h-1 bg-[#292925] rounded mt-1"><div className="h-full bg-ks-gold rounded" style={{width:meter(Number(value))+'%'}}/></div></div>)}</div><div className="text-[9px] text-ks-muted mt-3 uppercase">{club.pathwayFocus} focus · {club.transportSupport} transport support</div></button>)}</div></div></div>
}
